#first workflow
name :firstCI

on:[push]
jobs:
  build:
      runs-on:ubuntu-latest
      
      workflow_dispatch:

  steps:
    -uses:actions/checkout@v4
    -name : run something 
      run : echo hello world
